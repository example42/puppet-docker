# <%= @app %> Docker instance

description	"<%= @app %> Docker instance"

start on runlevel [2345]
stop on runlevel [!2345]

respawn
respawn limit 10 5
umask 022

exec <%= @app_settings['process_name'] %> <% if @app_settings['process_args'] %><%= @app_settings['process_args'] %><% end %>

